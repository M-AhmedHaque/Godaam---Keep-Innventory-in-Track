1. Authentication & User Management
📌 Who Does What?

Admin registers new users (either another admin or store managers).

Admin manages users (updates or deletes them).

All users (admin & store managers) can log in.

📂 Controller File: controllers/authController.js

POST /api/auth/register → Admin creates users.

POST /api/auth/login → All users log in.

📂 Controller File: controllers/userController.js

GET /api/users → Admin gets all users.

GET /api/users/:id → Admin retrieves user details.

PUT /api/users/:id → Admin updates user details.

DELETE /api/users/:id → Admin deletes users.

2. Product Management
📌 Who Does What?

Admin adds, updates, and removes products.

Store Managers can view products but cannot delete them.

📂 Controller File: controllers/productController.js

POST /api/products → Admin creates products.

GET /api/products → Both admin & store managers view products.

GET /api/products/:id → Retrieve product details.

PUT /api/products/:id → Admin updates product details.

DELETE /api/products/:id → Admin deletes a product.

3. Supplier Management
📌 Who Does What?

Admin manages suppliers.

Store Managers can only view suppliers.

📂 Controller File: controllers/supplierController.js

POST /api/suppliers → Admin adds a new supplier.

GET /api/suppliers → Both admin & store managers view suppliers.

GET /api/suppliers/:id → Retrieve supplier details.

PUT /api/suppliers/:id → Admin updates supplier details.	 

DELETE /api/suppliers/:id → Admin removes a supplier.

4. Store Management
📌 Who Does What?

Admin creates stores and assigns store managers.

Store Managers only manage stock in their assigned store.

📂 Controller File: controllers/storeController.js

POST /api/stores → Admin creates stores.

GET /api/stores → Both admin & store managers view stores.

GET /api/stores/:id → Retrieve store details.

PUT /api/stores/:id → Admin updates store details.

DELETE /api/stores/:id → Admin deletes a store.

5. Stock Management
📌 Who Does What?

Store Managers manage stock (stock in, update quantity, remove stock).

Admin has full visibility but doesn’t manage stock directly.

📂 Controller File: controllers/stockController.js

POST /api/stores/:storeId/stock → Store Manager adds stock.

GET /api/stores/:storeId/stock → Both admin & store managers view stock.

PUT /api/stores/:storeId/stock/:productId → Store Manager updates stock.

DELETE /api/stores/:storeId/stock/:productId → Store Manager removes a product from stock.

6. Stock Movement (Transactions)
📌 Who Does What?

Store Managers record stock-in, sales, and removals.

Admin views all stock movements.

📂 Controller File: controllers/stockMovementController.js

POST /api/stores/:storeId/stock-movement → Store Manager records stock movement.

GET /api/stores/:storeId/stock-movement → Both admin & store managers view stock movement logs.

GET /api/stores/:storeId/stock-movement/:id → Retrieve a specific stock movement.

7. Reports & Filters
📌 Who Does What?

Admin generates high-level reports across all stores.

Store Managers can only view reports related to their store.

📂 Controller File: controllers/reportController.js

GET /api/reports/stock-summary → Admin & store managers view stock summary.

GET /api/reports/sales-summary → Admin & store managers view sales reports.

GET /api/reports/top-products → Admin views top-selling products.

GET /api/reports/supplier-performance → Admin evaluates supplier performance.